#!/usr/bin/python3

""" A function that returns an instance of a class """


def is_same_class(obj, a_class):
    """ Defined function """
    if type(obj) == a_class:
        return (True)
    else:
        return (False)
